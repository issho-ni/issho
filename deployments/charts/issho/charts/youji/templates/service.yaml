apiVersion: v1
kind: Service
metadata:
  name: {{ include "youji.fullname" . }}
  labels:
    app.kubernetes.io/name: {{ include "youji.name" . }}
    helm.sh/chart: {{ include "youji.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: https
      protocol: TCP
      name: https
  selector:
    app.kubernetes.io/name: {{ include "youji.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}

apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ninshou.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "ninshou.name" . }}
    helm.sh/chart: {{ include "ninshou.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "ninshou.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
